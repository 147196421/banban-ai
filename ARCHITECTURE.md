# 办办AI 技术方案

## 当前实现

- 前端：React 19 + TypeScript；VPS 使用 Next.js，旧托管版本使用 Vinext/Vite
- UI：Tailwind CSS + 项目内置无障碍组件
- 部署：VPS 上运行 Next.js + 本机 SQLite + 独立 Node 检测服务；旧托管版仍可使用 Worker 兼容构建
- 当前页面：GPT 专用检测工作台，包含接口、密钥、模型发现、模型选择、推理等级与异步结果查询
- 模型发现：服务端调用上游 `/models`，过滤非 GPT 以及音频、图像等非文本模型
- 检测判定：VPS 网站默认调用满血 AI 公开 API；糖果题读取 `candy.status`，动画题读取 `assessment.quality`。旧版自有服务供既有任务或显式选择时使用
- 后台任务：客户端先生成幂等任务编号，服务端持久化后立即响应，并在响应结束后创建真实检测任务；浏览器进入后台不会中断模型调用
- 结果恢复：VPS 使用本机 SQLite 保存任务索引与已完成的报告 7 天；远程任务保留时间遵循满血 AI 公开文档；旧托管版使用 D1；客户端保存不含密钥的任务编号
- 安全状态：API Key 不写入浏览器缓存或数据库，仅在单次后台任务创建期间驻留内存

## 自有检测服务

`detector-server/` 是原有本机检测进程，只作基础结构检查。新任务默认由网站服务端请求满血 AI 的公开检测接口；浏览器切到后台不影响外部任务执行。网站保存已完成的报告，旧任务依照创建时的服务来源继续查询。详情见 [检测接口说明](self-hosted/REFERENCE-DETECTOR.md)。

VPS 在 `ai.banban.plus` 提供首页、自己的 `/v1/models`、`/v1/tests` 与截图代理；读取模型时调用用户模型 API，执行检测时将上游地址与 Key 交给满血 AI 的 `POST /api/v1/tests`，并轮询该服务返回的任务 ID。Nginx 直接代理本机网站，不代理旧托管网页。

本机结构检查不用于满血 AI 新任务的降智判定，满血 AI 的 `assessment.quality` 原样用作鹈鹕判定。单次作品仍不能证明模型身份或整体能力。旧托管站虽然配置了私有 `BANBAN_TEST_SERVICE_URL`，目前未取得其值，因此不能宣称旧站与满血 AI 是同一后端。

## 推荐演进路线

1. 检测记录：增加用户可控的本地历史或持久化报告。
2. 多次检测：同一配置连续运行三次，降低单次随机波动的影响。
3. 安全预览：HTML 作品在隔离 iframe 中展示，并限制脚本能力。
4. 数据层：保存任务、指标与报告；API Key 仅在任务执行期间驻留内存并对日志脱敏。
5. 定时监控：按模型创建计划任务，记录通过率、延迟、TPS 与费用趋势。
6. 公开报告：用户主动选择后才生成公开链接，不默认公开检测内容。

## GitHub 开源项目取舍

- Promptfoo：适合作为评测规则与断言思路来源，不直接复制其界面。
- LiteLLM：适合未来扩展多家模型供应商；第一版只做 OpenAI 兼容协议时不必引入。
- Langfuse：适合后期追踪与可观测性；基础版自行记录少量指标更轻。
- OpenAI Evals：适合参考标准化评测结构，不适合作为办办AI整站模板。

结论：保留自己的产品界面和任务系统，按需要引入开源评测能力，避免依赖不明来源的整站镜像。
